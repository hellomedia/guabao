{% extends '@admin/base.html.twig' %}

{% block content_title %}
    Cuisines bulk edit
{% endblock %}

{% block head_stylesheets %}
    {{ parent() }}
    <style>
    </style>
{% endblock %}

{% block main %}

    <div id="cuisines">
        {% for cuisine in cuisines %}
            {% include '@admin/cuisine/_quick_edit_form.html.twig'
                with { form: forms[cuisine.id] }
            %}
            <hr style="margin-top: 40px; margin-bottom: 30px;">
        {% endfor %}
    </div>

{% endblock %}

