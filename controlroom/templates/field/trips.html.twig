<div class="entity-list">
    {% if field.value|length > 0 %}
        {% for trip in field.value %}
            <a href="{{ path('controlroom_trip_detail', {entityId: trip.id}) }}">
                {{ trip|trans }}
            </a>
        {% endfor %}
    {% else %}
        <span class="badge badge-outline">{{ 'label.null'|trans({}, 'EasyAdminBundle') }}</span>
    {% endif %}
</div>