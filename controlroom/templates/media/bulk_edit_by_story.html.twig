{% extends '@EasyAdmin/page/content.html.twig' %}

{% block content_title %}
    Story: {{ story }}
{% endblock %}

{% block page_actions %}
    {% include '@controlroom/_top_links.html.twig' %}
{% endblock %}

{% block head_stylesheets %}
    {{ parent() }}
    <style>
        #trip, #story, #medias {
            margin-bottom: 40px;
        }
        #story {
            border: 1px solid lightgray;
            padding: 30px;
        }
        #trip {
            margin-top: 15px;
            font-size: 18px;
        }
    </style>
{% endblock %}

{% block main %}
    <div id="trip">
        Trip: <a href="{{ path('admin_media_bulk_edit_by_trip', {id: story.trip.id}) }}">{{ story.trip }}</a>
    </div>

    <div id="story">
        <h3>{{ story }}</h3>
        {% include "@controlroom/story/_quick_edit_form.html.twig" with { form: story_form, story: story } %}
    </div>

    <div id="medias">
        <h3>Medias</h3>
        {% for media in medias %}
            {% include '@controlroom/media/_quick_edit_form.html.twig'
                with { form: forms[media.id] }
            %}
            <hr style="margin-top: 40px; margin-bottom: 30px;">
        {% endfor %}
    </div>

{% endblock %}

{% block javascript %}
    {{ parent() }}
{% endblock %}

