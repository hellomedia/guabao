{# Important: Note the syntax with [] and only #}
{# Ignore global themes; use only EasyAdmin #}
{# important to avoid theme conflicts #}
{% form_theme form with ['@EasyAdmin/crud/form_theme.html.twig'] only %}

<div id="media_{{ media.id }}" data-controller="ajax">

    <form
        name="trip_media_edit_form_{{ media.id }}"
        data-ajax-target="form"
        data-action="submit->ajax#submit"
        action="{{ path('admin_trip_media_edit', {id: media.id}) }}"
        method="post"
        style="margin-bottom: 20px"
    >

        {# container for form content replaced in ajax #}
        <div data-ajax-target="container" style="columns: 400px 2;column-gap: 50px;">

            <img
                style="height: 300px; width: auto;" 
                src="{{ uploaded_asset(media)|imagine_filter('thumb_card_square') }}"
            />

            <div style="margin-top: 15px; margin-bottom: 10px">
                {{ form_row(form.descriptionEn) }}
                {{ form_row(form.descriptionFr) }}
            </div>
            
            {{ form_rest(form) }}
        
        </div>

        {# button and spinner live outside of container replaced in ajax #}
        <div style="position: relative; display: inline-block; margin-top: 30px;">
            <button type="submit" style="padding: 10px 40px;font-size: 16px;"
                class="btn btn-primary btn-lg"
                data-ajax-target="submitBtn"
            >
                Save
            </button>
            
            <div data-ajax-target="spinner" class="spinner-wrapper hidden">
                <div class="spinner" aria-hidden="true"></div>
            </div>
        </div>

    </form>

</div>

