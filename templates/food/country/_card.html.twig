<div class="card card-country">

	<h3 class="card-title">
		<a href="{{ path('food_countries_country', {slugEn: country.slugEn}) }}">
			{{ country|trans }} <span class="count">({{ food_count }})</span>
		</a>
	</h3>

</div>

