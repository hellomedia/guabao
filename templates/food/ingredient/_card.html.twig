<div class="card card-ingredient">

    <h3 class="card-title">
        <a href="{{ path('food_by_ingredient_ingredient', {slugEn: ingredient.slugEn}) }}">
            {{ ingredient|trans }} <span class="count">({{ food_count }})</span>
        </a>
    </h3>

</div>
