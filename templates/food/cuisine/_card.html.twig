<div class="card card-cuisine">

    <h3 class="card-title">
		<a href="{{ path('food_by_cuisine_cuisine', {slugEn: cuisine.slugEn}) }}">
            {{ cuisine|trans }} <span class="count">({{ food_count }})</span>
        </a>
    </h3>

</div>
