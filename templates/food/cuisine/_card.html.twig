<div class="card card-cuisine">

    <h3 class="card-title">
		<a href="{{ path('food_cuisines_cuisine', {slugEn: cuisine.slugEn}) }}">
            {{ cuisine|trans }} <span class="count">({{ food_count }})</span>
        </a>
    </h3>

</div>
