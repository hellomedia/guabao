<div class="card card-tag">

	<h3 class="card-title">
		<a href="{{ path('food_tags_tag', {slugEn: tag.slugEn}) }}">
			{{ tag|trans }} <span class="count">({{ food_count }})</span>
		</a>
	</h3>

</div>

