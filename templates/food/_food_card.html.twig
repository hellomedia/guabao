<div class="card card-food">
    {% set cover = food.cover %}
    {% if cover is not null %}
        <a href="{{ url }}" class="card-image-wrapper">
            <img 
                src="{{ uploaded_asset(cover)|imagine_filter('thumb_4_3_640') }}"
                srcset="{{ uploaded_asset(cover)|imagine_filter('thumb_4_3_640') }} 640w,
                    {{ uploaded_asset(cover)|imagine_filter('thumb_4_3_1280') }} 1280w"
                sizes="(max-width: 480px) 100vw,
                    (max-width: 900px) 50vw,
                    320px"
                width="800" {# keep 800x600. flex sizing depends on those values #}
                height="600"
                {{ loop.index ?? 0 > 8 ? 'loading="lazy"' }}
            />
        </a>
    {% else %}
        <a href="{{ url }}" class="card-image-wrapper">
            {% include 'media/_placeholder_image.html.twig' %}
        </a>
    {% endif %}

    <div class="card-info">
        <h3 class="card-title">
            {% if food.originalName %}
                <span class="card-title-main">
                    <a href="{{ url }}">{{ food|trans }}</a>
                </span>
                <span class="card-title-sub">
                    <a href="{{ url }}">{{ food.originalName }}</a>
                </span>

            {% else %}
                <span class="card-title-main is-only">
                    <a href="{{ url }}">{{ food|trans }}</a>
                </span>
            {% endif %}
        </h3>


        <div class="card-bottom invisible-scroll-force-width">

            {% if (short_version ?? false) == false %}
                <div class="card-ratings">
                    {% include 'food/_rating.html.twig' %}
                </div>
            {% endif %}

            <div class="card-tags tags invisible-scroll invisible-scroll-force-width fade-end">

                {% for cuisine in food.cuisines %}
                    <a
                        class="tag food-tag"
                        href="{{ path('food_cuisines_cuisine', {slugEn: cuisine.slugEn}) }}"
                    >
                        {{ cuisine|trans }}
                    </a>
                {% endfor %}
                {% for tag in food.tags %}
                    <a
                        class="tag food-tag"
                        href="{{ path('food_tags_tag', {slugEn: tag.slugEn}) }}"
                    >
                        {{ tag|trans }}
                    </a>
                {% endfor %}

            </div>
        </div>

    </div>
</div>