{% include 'trip/_trip_overview_menu.html.twig' %}

<div id="rating">
	{% include 'trip/_rating.html.twig' %}
</div>

{% if trip.description(app.locale) is not empty %}
    <div id="description">
        {{ trip.description(app.locale)|nl2br }}
    </div>
{% endif %}

{# {% if trip.cover %}
	<div id="cover">
		<img src="{{ uploaded_asset(trip.cover)|imagine_filter('thumb_large') }}"
            width="{{ trip.cover.width }}"
            height="{{ trip.cover.height }}"
        />
	</div>
{% endif %} #}

{% include "trip/_stories.html.twig" with {'stories': trip.stories} %}


{% if medias is not empty %}
    <h2>The rest</h2>
    {% include "trip/_gallery.html.twig" %}
{% endif %}

{% include 'trip/_trip_overview_menu.html.twig' with { position: 'bottom' } %}

