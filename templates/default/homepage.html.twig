{% extends 'layout/default.html.twig' %}

{% block title %}
	{{ 'Guabao'|trans }}
{% endblock %}

{% block body_extra_classes 'with-cards' %}

{% block content %}

<style>
    .homepage-section {
        margin: 30px 0;
    }
    .section-title {

    }
    .section-content {

    }
    .section-item {

    }
</style>

    <div class="homepage-section">
        <h2 class="section-title">Top Slow Travel trips</h2>
        <div class="section-content">
            <div class="cards cards-horizontal">
                {% for trip in top_slow_travel_trips %}
                    {% include 'trip/_card.html.twig' %}
                {% endfor %}
            </div>
        </div>
    </div>

    <div class="homepage-section">
        <h2 class="section-title">Top Hiking trips</h2>
        <div class="section-content">
            <div class="cards cards-horizontal">
                {% for trip in top_hiking_trips %}
                    {% include 'trip/_card.html.twig' %}
                {% endfor %}
            </div>
        </div>
    </div>

    <div class="homepage-section">
        <h2 class="section-title">Top Foods</h2>
        <div class="section-content">


        </div>
    </div>
{% endblock %}
